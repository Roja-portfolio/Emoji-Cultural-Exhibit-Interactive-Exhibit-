Open terminal (in VS Code or Git Bash), then:

cd path/to/emoji-exhibit
git init
git add .
git commit -m "Initial commit: Emoji Exhibit"
gh repo create emoji-exhibit --public --source=. --remote=origin
git push -u origin main
